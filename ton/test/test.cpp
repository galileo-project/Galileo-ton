/*
    class TestTask: public Task {
    }

    Chan chan;
    TestTask task = new TestTask();
    Worker worker;
    worker.add(task);
    Runner runner = Runner(chan);
    runner.add(worker);
    
    Msg msg;
    msg.add("something");
    chan.send(worker, msg);
    chan.broadcast(msg);
*/

#include "common.h"
#include "test.h"


int main() {
    
}