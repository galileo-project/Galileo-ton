/*
    class TestTask: public Task {
    }

    Chan chan;
    Worker worker;
    Runner runner = Runner(chan);
    runner.add(worker);
    
    TestTask task = new TestTask();
    
    Msg msg = new Msg();
    msg.set_task(task);
    chan.send(worker, msg);
    chan.broadcast(msg);
*/

#include "common.h"
#include "test.h"

int main() {
    
}